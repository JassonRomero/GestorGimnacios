<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cr.ac.ucr.laboratorio2_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReservasListActivity"></activity>
        <activity android:name=".Reservas_Cliente_Activity" />
        <activity android:name=".ClasesCliente" />
        <activity android:name=".SecionesClienteActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".LoginClienteActivity" />
        <activity android:name=".ClasesAdministrador" />
        <activity android:name=".SecionesActivity" />
        <activity android:name=".ActualizarNegocio" />
        <activity android:name=".InicioAdministrador" />
        <activity android:name=".LoginCenter" />
        <activity android:name=".StudentsListActivity" /> <!-- <activity android:name=".ui.LoginCenter" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.login.LoginCenter" -->
        <!-- android:label="@string/title_activity_login_center" /> -->
        <!-- <activity android:name=".StudentsListActivity" /> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>